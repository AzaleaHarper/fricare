import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — add widget tests here once the app is stable.
  testWidgets('smoke test', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
